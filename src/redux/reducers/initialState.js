const initialState = {
  defaultSettings: {
    darkMode: true,
    showBackgroundImage: true,
    showGridOverlay: true,
    showCorrectPlacement: false,
    defaultGridSize: 8
  }
};

export default initialState;