import { ADD_FAVOURITE_LIBRARY_IMAGE } from '../../constants/actionTypes';

const addFavouriteLibraryImage = newFavourite => ({ type: ADD_FAVOURITE_LIBRARY_IMAGE, newFavourite });

export default addFavouriteLibraryImage;