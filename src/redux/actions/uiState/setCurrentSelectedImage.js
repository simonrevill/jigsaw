import { SET_CURRENT_SELECTED_IMAGE } from '../../constants/actionTypes';

const setCurrentSelectedImage = data => ({ type: SET_CURRENT_SELECTED_IMAGE, data });

export default setCurrentSelectedImage;